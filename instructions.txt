1. git init
2. git add .
3. git branch -M main
4. git remote add origin git@github.com:marcusashmond/Auth_service.git
5. git commit -m 'init(project): initial commit'
6. git push -u origin main

pip3 install -r requirements.txt

pip install --upgrade pip

START HERE

git switch -c feat/config

git add .

git commit -m 'feat(config): add config file'

git push -u origin feat/config

END HERE